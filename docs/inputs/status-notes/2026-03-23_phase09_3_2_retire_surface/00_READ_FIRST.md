# Phase 09.3.2 Status Note

Date: `2026-03-23`
Scope: post-`09.3.2` bounded portal-local state-machine continuation
Repo surface: `zpe-video`
Authority surface: outer workspace `.gpd` plus `zpe_video_lab`

## Purpose

Give engineering and science a compact current-state note after the `09.3.2` bounded continuation on the sparse VIRAT facility-crossing surface.

## Canonical Order

1. `01_STATUS.md`
2. `02_KEY_TABLES.md`

## Bottom Line

`09.3.2` closes this surveillance lane as `retire_surface`.

The best defended full-surface state machine improves the `09.3.1` point from `34.38%` to `37.50%` non-event suppression at the same `96.30%` recall floor, but it still fails one clip at `5/6` recall, fails multiple leave-one-clip-out holdouts, and no entering-only or exiting-only split reaches a defended local gate.

That means the sparse VIRAT surveillance surface is now retired as a promotable wedge. The next honest move is a different artifact or consumer class, not more tuning on this lane.
