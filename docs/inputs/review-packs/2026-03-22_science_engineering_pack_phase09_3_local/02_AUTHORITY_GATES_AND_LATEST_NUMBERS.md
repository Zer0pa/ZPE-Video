# Authority Gates And Latest Numbers

## Governing Gate

The current subordinate wedge gate remains:

- `>= 50%` suppression
- `>= 95%` retained utility

Nothing in the current `09` family clears that gate.

## Broad Temporal Reference

Phase `09-09` stayed bounded on defended `MOT17Det` time.

- YOLOv8m best safe side: `35.34%` suppression at about `96.50%` retained utility
- RT-DETR-L best safe side: `33.33%` suppression at about `96.96%` retained utility
- Aggressive side reached near-half suppression only by dropping retained utility to about `90-92%`

## Primitive-Layered Reference

Phase `09.2` also stayed bounded on the same defended temporal surface.

- YOLOv8m best safe side: `39.93%` suppression at `95.72%` retention
- RT-DETR-L best safe side: `39.12%` suppression at `96.11%` retention
- No layered policy produced a surviving near-half regime

## Narrow Surveillance Result

Phase `09.3` moved to a different evaluator:

- surface: sparse VIRAT facility-entry and exit surveillance clips
- positive class: sampled frames with active facility events
- negative class: sampled frames from the same clips with no active facility event
- score: primitive packet topology delta between consecutive sampled packets

Best-safe `09.3` point:

- threshold: `0.059758`
- facility-event recall: `100%`
- non-event suppression: `14.67%`
- true positives: `5`
- false positives: `64`

## What The Numbers Mean

The packet remains measurable. It still does not become a wedge, even after narrowing to the strongest currently owned surveillance slice.
