# Next Experiment Options

## Option 1

Find a tighter partner surface inside surveillance instead of using all sparse facility-event positives as one class.

Examples:

- a narrower event subtype
- a single-camera slice with more stable geometry
- one operational micro-contract such as portal crossing or facility ingress only

Why:

`09.3` suggests the broad “facility event active” label is still too heterogeneous.

## Option 2

Change the artifact class more materially.

Examples:

- persistence contract change
- packet enrichment tied to the narrow surface
- a direct downstream consumer of primitive state that is not just delta routing

Why:

All measured routing-style surfaces are bounded on the current packet stream.

## Option 3

Retire surveillance triage as the wedge hypothesis if no tighter owned surface can be defended.

Why:

The program should not stay trapped in evaluator churn if the narrow surveillance lane keeps returning bounded mixed signal.

## My Current Preference

Option `2`, unless the team can define a much tighter, more operational surveillance micro-surface than the current sparse facility-event slice.
