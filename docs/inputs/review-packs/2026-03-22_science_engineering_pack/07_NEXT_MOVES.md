# Next Moves

Date: `2026-03-22`

## What I Would Do Next

I would not spend the next cycle tuning the same layered thresholds. That path looks locally defensible but strategically weak.

I would choose one of these two substantive branches:

## Branch A: New Defended Partner Surface

Goal: test whether the packet has real commercial value on a more product-shaped evaluator than the current detector-invocation gate.

Why:

- `09-09` and `09.2` both show bounded control-plane value
- if the current gate is too indirect, a better defended evaluator could expose real value that detector suppression alone misses
- if no value appears there either, the lane gets a cleaner falsification

Requirements:

- one explicit evaluator contract
- same honesty discipline as the current gate
- no narrative promotions without measured closure

## Branch B: New Primitive Artifact Or Persistence Contract

Goal: change the information substrate enough that the scorer is not merely reordering the same packet.

My preferred version of this branch:

- contour-owned persistence rather than weaker block-style persistence
- explicit cut handling and audio-bearing clips before claiming any transition or audio-sync utility
- packet state that survives hard cuts and still beats temporal-only baselines

## Supporting Engineering I Would Approve

- persist full detector-surface summaries in a cheaper reusable form
- mirror compact phase evidence into the inner repo automatically
- keep lightweight import and codec tests green after every repo cleanup

## Moves I Would Explicitly Reject

- reopening `9B` from the current bounded evidence
- more threshold shopping on `09.2`
- narrating Seedance as if it already transferred a commercial contract
- calling the current packet a wedge because it has bounded safe-side gains
