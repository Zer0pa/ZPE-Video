# Authority Gates And Latest Numbers

## Governing Subordinate Gate

The current subordinate wedge gate remains:

- `>= 50%` suppression
- `>= 95%` retained utility

Nothing in the current `09` family clears that gate cleanly.

## Broad Temporal References

Phase `09-09` stayed bounded on defended `MOT17Det` time.

- YOLOv8m best safe side: about `35.34%` suppression at about `96.50%` retained utility
- RT-DETR-L best safe side: about `33.33%` suppression at about `96.96%` retained utility
- aggressive side reached near-half suppression only by dropping retained utility to about `90-92%`

Phase `09.2` also stayed bounded on the same defended temporal surface.

- YOLOv8m best safe side: `39.93%` suppression at `95.72%` retention
- RT-DETR-L best safe side: `39.12%` suppression at `96.11%` retention
- no layered policy produced a surviving near-half regime

## Narrow Surveillance References

### Phase `09.3`

- surface: sparse VIRAT facility-entry and exit surveillance clips
- score: full-frame primitive packet topology delta
- best-safe point: `100%` recall, `14.67%` non-event suppression

### Phase `09.3.1`

- surface: exact same sparse VIRAT facility-event cohort
- aperture: event-linked actor portal from VIRAT events, objects, and mappings
- score family: primitive-local payload, contour, stroke, point, and temporal-delta features

Best simple rule:

- `stroke_delta >= 0.006493506493506494`
- recall: `96.30%`
- suppression: `31.25%`

Best aggregate rule:

- `stroke_delta >= 0.006493506493506494 AND point_delta <= 0.1233644859813084`
- recall: `96.30%`
- suppression: `34.38%`
- improvement over `09.3`: `+19.71` suppression points, or `2.34x`

## What The Numbers Mean

- portal anchoring is a real improvement
- the strongest surviving local signal is primitive-local stroke change
- the local gate is still red because `34.38%` is well below the `>= 50%` target
- the global rule is also not stable enough because one clip falls to `88.89%` recall
