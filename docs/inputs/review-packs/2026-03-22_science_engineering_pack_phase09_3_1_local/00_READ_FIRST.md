# Science And Engineering Pack

Date: `2026-03-22`
Scope: post-`09.3.1` portal-local update
Repo surface: `zpe-video`
Local authority surface: outer workspace `.gpd` plus `zpe_video_lab`

## Purpose

Give the science and engineering team a compact, current, falsifiable packet after the `09.3.1` portal-anchored primitive consumer probe on sparse VIRAT facility-crossing events.

## What Is Canonical

1. `zpe-video` is the GitHub-tracked handoff repo.
2. The run-of-record for `09.3.1` was executed locally in the outer workspace lab.
3. When a narrative summary disagrees with a run-of-record JSON, trust the JSON, the phase summary, and the explicit decision note.

## Fast Read Order

1. `01_EXECUTIVE_STATUS_AND_POV.md`
2. `02_AUTHORITY_GATES_AND_LATEST_NUMBERS.md`
3. `03_PHASE_09_3_1_PORTAL_LOCAL_CONSUMER.md`
4. `07_NEXT_EXPERIMENT_OPTIONS.md`
5. `09_POD_SHUTDOWN_AND_REENGAGEMENT.md`

## Bottom Line

The packet is still the right central object. The wedge is still not open. `09.3.1` is a real improvement over `09.3`: portal-local primitive consumption lifts the narrow sparse-facility surface from `14.67%` to `34.38%` non-event suppression at `96.30%` recall. That is materially better and still bounded because one clip falls to `88.89%` recall and leave-one-clip-out splits remain unstable.
