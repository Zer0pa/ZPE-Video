# Next Experiment Options

## Option 1

Stay in surveillance, but tighten the local contract again.

Examples:

- ingress only or egress only
- one camera geometry family only
- explicit crossing phases instead of generic event-active frames

Why:

`09.3.1` suggests the current label is still too heterogeneous even after portal anchoring.

## Option 2

Keep the same sparse facility surface and upgrade the consumer from a shallow rule set to a portal-local primitive state machine.

Examples:

- event-phase logic
- short-memory state transitions
- primitive-layered portal logic instead of a single or paired threshold

Why:

This is the cleanest continuation of the current result. The local signal is real; it is the consumer stability that still fails.

## Option 3

Retire surveillance triage as the wedge hypothesis if the team cannot define a tighter micro-surface or a sharper portal-local consumer.

Why:

The program should not stay trapped in narrow-surface churn if the remaining instability is structural rather than incidental.

## My Current Preference

Option `2`.

The strongest honest next move is a portal-local primitive state machine for facility crossing phases. It stays faithful to P8, preserves the narrow surface that actually improved, and attacks the real residual directly.
