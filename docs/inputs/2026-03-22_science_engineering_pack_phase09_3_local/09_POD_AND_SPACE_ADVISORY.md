# Pod And Space Advisory

## Disk

Current local headroom observed during packaging:

- approximately `61 GiB` available on the Mac data volume

This pack is Markdown-only and is negligible in size. I did not duplicate large datasets or reports into the handoff pack.

## Pod Need

For immediate continuation of the current `09.3` narrow-surface lane:

- pod is **not required**

For heavier future work, pod may still be useful or needed in practice for:

- broader defended detector reruns on `MOT17Det`
- larger-scale multi-surface replay
- heavier training or feature-adapter experiments

## Operational Advice

- keep the outer workspace and `zpe_video_lab` directory intact on the Mac
- do not assume the GitHub mirror alone is enough to rerun `09.3`
- if the next phase stays near the current narrow surveillance lane, local Mac continuation is viable
